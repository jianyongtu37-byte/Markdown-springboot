
spring.application.name=markdown-app

# ?????
server.port=${SERVER_PORT:8080}
server.servlet.context-path=/

# MySQL ???????
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:markdown_db}?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========== MyBatis-Plus ?? ==========
mybatis-plus.mapper-locations=classpath*:/mapper/**/*.xml
mybatis-plus.type-aliases-package=com.nineone.markdown.entity
mybatis-plus.configuration.map-underscore-to-camel-case=true
# SQL ?????? StdOutImpl ????????? logging.level ??
# ???? SQL???? mapper ?????? debug ??
# mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# ????
mybatis-plus.global-config.db-config.logic-delete-field=deleted
mybatis-plus.global-config.db-config.logic-delete-value=1
mybatis-plus.global-config.db-config.logic-not-delete-value=0
mybatis-plus.global-config.db-config.insert-strategy=not_null
mybatis-plus.global-config.db-config.update-strategy=not_null



# ========== ???? ==========
# SQL ????? warn ?????? SQL ??
# ??????? debug ??? SQL????StdOutImpl ???????????
logging.level.com.nineone.markdown.mapper=warn
logging.level.root=${LOG_LEVEL:INFO}
# Elasticsearch ??
logging.level.org.springframework.data.elasticsearch.client.WIRE=TRACE
logging.level.tracer=TRACE
logging.level.co.elastic.clients=DEBUG

# ========== Redis ?? ==========
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.database=${REDIS_DATABASE:0}
spring.data.redis.timeout=5000ms
spring.data.redis.connect-timeout=2000ms

# ========== Elasticsearch ?? ==========
spring.elasticsearch.rest.uris=http://${ES_HOST:localhost}:${ES_PORT:9200}
spring.elasticsearch.rest.username=${ES_USERNAME:}
spring.elasticsearch.rest.password=${ES_PASSWORD:}

# ========== JWT ?? ==========
jwt.secret=${JWT_SECRET:}
jwt.expiration=${JWT_EXPIRATION:86400}

# ========== DeepSeek AI ?? ==========
# ??? https://platform.deepseek.com/ ?? API Key ?????
ai.deepseek.api-key=${DEEPSEEK_API_KEY:}
ai.deepseek.api-url=${DEEPSEEK_API_URL:https://api.deepseek.com/v1}
ai.deepseek.model=${DEEPSEEK_MODEL:deepseek-chat}
ai.deepseek.timeout-seconds=${DEEPSEEK_TIMEOUT_SECONDS:30}
ai.deepseek.max-tokens=${DEEPSEEK_MAX_TOKENS:500}

# ========== ??? ==========
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=20
spring.task.execution.pool.queue-capacity=100
spring.task.execution.thread-name-prefix=async-task-
spring.task.async.enabled=true

# ========== ???? ==========
# QQ?? SMTP ???: smtp.qq.com ??587(TLS)?465(SSL)
# ?????QQ?????????QQ??
# Gmail SMTP ???: smtp.gmail.com ??587(TLS)?465(SSL)
# ?????Google??????
spring.mail.host=${MAIL_HOST:smtp.qq.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.protocol=${MAIL_PROTOCOL:smtp}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.ssl.enable=${MAIL_SSL_ENABLE:false}
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_CONNECTION_TIMEOUT:5000}
spring.mail.properties.mail.smtp.timeout=${MAIL_TIMEOUT:5000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_WRITETIMEOUT:5000}
spring.mail.default-encoding=UTF-8

# ========== ???? ==========
# ??????? ImageServiceImpl ???? 5MB ??????
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ========== Jackson ??:?????? ==========
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=GMT+8
spring.jackson.serialization.write-dates-as-timestamps=false

# ========== ???? ==========
# ??URL????????
app.url=${APP_URL:http://localhost:8080}

# ========== 评论审核配置 ==========
# 评论是否自动通过审核（true-自动通过, false-需要审核）
app.comment.auto-approve=${COMMENT_AUTO_APPROVE:true}
# 敏感词处理策略（REJECT-拒绝发布, FLAG-标记替换后提交）
app.comment.sensitive-word-action=${COMMENT_SENSITIVE_WORD_ACTION:REJECT}

# ========== ??/?? ==========
# ????????
app.export.dir=${APP_EXPORT_DIR:exports}
# ????????
app.backup.retention-days=${APP_BACKUP_RETENTION_DAYS:30}
# ????????cron??? (???? 2:00)
app.backup.cron=${APP_BACKUP_CRON:0 0 2 * * ?}

# ========== ???? ==========
# ???? Elasticsearch ????ES???false
spring.data.elasticsearch.repositories.enabled=${SPRING_DATA_ELASTICSEARCH_REPOSITORIES_ENABLED:true}

# ?? Redis ??
spring.data.redis.repositories.enabled=false

# ========== ??? ==========
# ?????
spring.devtools.restart.enabled=true
spring.devtools.restart.additional-paths=src/main/java
spring.devtools.restart.exclude=static/**,public/**

spring.config.import=optional:file:.env[.properties]
# ========== ???? ==========
# ?? SecurityConfig.java ?? CORS ??
# ???? http://localhost:3000, http://localhost:8080

# ========== ???? ==========
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# ========== ????? ==========
# Nacos ?????
spring.cloud.nacos.discovery.server-addr=${NACOS_SERVER:localhost:8848}
spring.cloud.nacos.discovery.namespace=${NACOS_NAMESPACE:public}

# Sentinel ????
spring.cloud.sentinel.transport.dashboard=${SENTINEL_DASHBOARD:localhost:8089}
spring.cloud.sentinel.eager=true
spring.cloud.sentinel.feign.enabled=true

# ========== DeepSeek ???? ==========
# ????????
ai.deepseek.polish-max-input-length=${DEEPSEEK_POLISH_MAX_INPUT_LENGTH:8000}
# ??????token?
ai.deepseek.polish-max-tokens=${DEEPSEEK_POLISH_MAX_TOKENS:4000}
# ?????? auto, academic, formal, business, casual, creative, technical, marketing
ai.deepseek.polish-default-style=${DEEPSEEK_POLISH_DEFAULT_STYLE:auto}
# ?????? auto, friendly, professional, persuasive, neutral, enthusiastic, authoritative
ai.deepseek.polish-default-tone=${DEEPSEEK_POLISH_DEFAULT_TONE:auto}
# ????????? true: ???????, false: ???????
ai.deepseek.polish-allow-custom=${DEEPSEEK_POLISH_ALLOW_CUSTOM:true}
# ??????
ai.deepseek.polish-timeout-seconds=${DEEPSEEK_POLISH_TIMEOUT_SECONDS:60}


# ========== RAG 知识问答服务配置 ==========
rag.agent.url=${RAG_AGENT_URL:http://localhost:8085}
rag.agent.timeout-seconds=${RAG_AGENT_TIMEOUT:60}
rag.agent.sync-on-save=${RAG_SYNC_ON_SAVE:true}
# 索引对账定时任务 cron 表达式（默认每天凌晨 3:00）
app.index.reconcile.cron=${APP_INDEX_RECONCILE_CRON:0 0 3 * * ?}

# ========== Feign 超时配置 ==========
# RAG 服务 Feign 客户端超时（connectTimeout=5s, readTimeout=30s）
feign.client.config.rag-agent.connectTimeout=${FEIGN_RAG_CONNECT_TIMEOUT:5000}
feign.client.config.rag-agent.readTimeout=${FEIGN_RAG_READ_TIMEOUT:30000}
# 默认 Feign 超时配置
feign.client.config.default.connectTimeout=${FEIGN_DEFAULT_CONNECT_TIMEOUT:5000}
feign.client.config.default.readTimeout=${FEIGN_DEFAULT_READ_TIMEOUT:30000}
